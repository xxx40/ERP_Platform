# D13 验证策略

## 目标

把设计决策转成可执行的验证计划，覆盖自动化、手动和必要的环境验证。

## 输出契约

- 自动化测试：单测、集成、契约、组件、端到端、快照、类型测试。
- 手动验证：必须人工确认的 UI、平台、权限、发布或环境场景。
- 负向验证：错误输入、失败路径、权限拒绝、兼容旧数据。
- 验证命令：能在当前仓库执行的命令优先。
- 覆盖映射：关键设计点对应哪条验证。

## 完成检查

- [ ] 每个 `core` 维度至少有一个验证信号。
- [ ] 手动验证项说明为什么不能自动化。
- [ ] 验证命令或步骤能被后续 tasks 引用。
