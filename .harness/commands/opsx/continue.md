---
name: "OPSX: Continue"
description: Create the next planning artifact without crossing the apply boundary
category: Workflow
tags: [workflow, artifacts, experimental]
---

Load and follow the `openspec-continue-change` skill.

Pass the optional change name and relevant hot conversation context. Return the created artifact,
updated status, blockers or apply prompt, and the skill's stop reason.
