schema: harness-lite

context: |
  全局规则：
  - 模板章节按需取用；省略无关内容，不写空占位。
  - 若需调整已完成内容，先更新对应 artifact，再继续下一环节。
  - Apply 前不触发独立 Review；调查、挑战与校验由当前 Agent 完成，用户明确要求审查时例外。
  - UML、Mermaid 或架构图：节点标签、连线说明、分组标题使用中文描述，仅语法关键字和代码标识符保留英文。
