# 头脑风暴

<!-- 保留会影响范围、方案或验收的结论，省略无关内容。使用兼容 Markdown 的短段落、
     表格、普通引用块和 Mermaid 建立视觉层级；引用块只承载目标、关键判断或流程结论。 -->

## 目标

> **🎯 目标结果：** <!-- 一句话说明要解决的问题，以及完成后最重要的可观察结果。 -->

## 一图读懂

<!-- 多阶段、分支、异步、状态流转或涉及 3 个以上组件时保留本节，否则删除整个本节。
     使用 Mermaid 展示一条全局主线；图后用 1–3 句正文保留可检索的关键结论。
     图中对应核心用例的节点显示同一 UC-* 编号，并用统一 usecase 类突出；普通流程节点保持
     中性样式，不只靠颜色表达关联。flowchart 使用以下稳定语法；其他图型至少保留编号，
     并只使用该图型原生支持的强调方式：
     A["UC-01 用例结果"]:::usecase
     classDef usecase fill:#E8F3FF,stroke:#1677FF,stroke-width:2px,color:#0F3B67; -->

> **💡 核心判断：** <!-- 写出图中最重要、最影响后续方案或验收的结论。 -->

## 现状与问题

<!-- 当前行为、真实痛点和必要的源码调查结论；不要重复无关背景。
     只有 1–3 个简单问题时使用短列表；问题较多时按问题域压缩到下表。 -->

| 问题域 | 已确认事实 | 直接影响 |
|---|---|---|

## 核心用例

<!-- 每个用例使用 UC-01、UC-02 等稳定编号，图和后续 design 复用同一编号；新增用例追加编号，
     不因排序变化重编号。按需增删行；至少覆盖主成功场景，以及会改变方案或验收的失败、权限、
     空态和兼容边界。UI/交互变更还要覆盖关键视图状态与交互结果。超过 8 行或混合多类场景时，
     使用三级标题分成主流程、异常与恢复等真实分组，不把所有用例塞进一张表。 -->

| 用例 ID | 角色/调用方 | 触发场景 | 预期结果 | 关键边界 |
|---|---|---|---|---|

## 范围与验收

<!-- 按主题拆行，避免把全部 In Scope / Out of Scope 压进两个超长单元格。 -->

| 范围主题 | In Scope | Out of Scope |
|---|---|---|
| <!-- 主题 --> | <!-- 本次明确交付的能力、场景或行为。 --> | <!-- 明确不做的内容及必要原因。 --> |

### 验收条件

<!-- 使用编号列表；每项以简短的加粗场景标签开头，并能观察成功或失败。 -->

## 架构与影响概览

<!-- 从需求视角说明受影响边界，不提前展开详细设计。按需增删列与行。
     “一图读懂”已经回答相同问题时不重复配图；只有需要表达不同关系时再补 Mermaid。 -->

| 区域/模块 | 当前职责或行为 | 本次影响 |
|---|---|---|

## 方案方向与取舍

<!-- 已确认的主方向、关键机制及替代方向取舍。决策较多或存在真实候选时使用下表；
     单一简单方向直接写短段落并删除空表。 -->

| 决策主题 | 选择 | 依据与代价 |
|---|---|---|

## 约束、风险与决策

<!-- 只记录会影响实施的兼容、迁移、安全、外部依赖等约束和风险，并给出对应决策或控制方式。 -->

| 类型 | 约束或风险 | 决策或应对 |
|---|---|---|

## 流程选择

> **✅ 流程结论：** <!-- Direct / Lite / Full 的判断、Agent 建议和用户最终选择；无升级风险时简述即可。 -->
